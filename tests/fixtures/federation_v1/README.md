# Federation V1 Golden Wire Fixtures 01

Deterministic test-only Ed25519 seeds and SFDJ-1 bytes. Private seeds are synthetic and must never be used outside tests. The manifest records SHA-256 for every fixture artifact except itself.
