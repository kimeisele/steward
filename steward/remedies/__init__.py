"""Steward-owned CST remedies — precision over aggression.

Uses Shuddhi Engine (LibCST) for surgical code transformation,
but with Steward-quality classification that distinguishes between
intentionally silent catches and truly dangerous silent failures.

Register via: engine.add_remedy_path(Path("steward/remedies"))
"""
